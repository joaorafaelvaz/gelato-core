# PostgreSQL
POSTGRES_USER=gelato
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=gelatocore

# API / Prisma
DATABASE_URL=postgresql://gelato:change-me-strong-password@postgres:5432/gelatocore?schema=public
JWT_SECRET=change-me-long-random-secret-at-least-32-chars
API_PORT=4000
API_HOST=0.0.0.0

# BackOffice / POS
VITE_API_URL=/api

# Domain / SSL (nginx production)
DOMAIN=gelato.example.com
API_PORT=4000

# Backups
BACKUP_RETENTION_DAYS=7
